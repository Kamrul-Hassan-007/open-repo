Extract them both and put the wallpaper folder in Graphite theme folder if you want to  use those walls..if not leave it..To install Graphote just run install.sh in graphite theme folder.

##

Graphite is a GTK theme..Because Hypeland uses GTK application not QT.
